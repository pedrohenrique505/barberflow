export function App() {
  return (
    <main className="min-h-screen bg-background px-6 py-10 text-text-primary">
      <section className="mx-auto max-w-5xl">
        <p className="text-sm font-medium text-text-secondary">BarberFlow</p>
        <h1 className="mt-3 text-3xl font-semibold tracking-normal">
          Fundação do sistema de agendamentos
        </h1>
        <p className="mt-4 max-w-2xl text-base leading-7 text-text-secondary">
          Estrutura inicial preparada para evoluir o painel da barbearia e a
          página pública de reservas.
        </p>
      </section>
    </main>
  );
}
